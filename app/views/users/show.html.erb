<% provide(:title,@user.name) %>
<div>
    <% if @user.notes.any? %>
      <h3>Notes (<%= @user.notes.count %>)</h3>
          <ol class="notes">
            <%= render @notes %>
          </ol>
      <%= will_paginate @notes %>
    <% end %>
</div>